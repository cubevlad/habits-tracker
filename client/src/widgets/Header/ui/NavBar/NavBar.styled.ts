import { Stack } from '@mui/material'
import styled from 'styled-components'

export const StyledNavBarWrapper = styled(Stack)`
  align-items: center;
`

export const StyledNavLinkWrapper = styled(Stack)`
  flex: 1 1 auto;
`
